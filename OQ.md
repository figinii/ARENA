why layer norm makes the loss space smoother? -> visualize it
why embdding matri and unembedding aren't just look up tables, but instead they are trainable parameters?
why the position embeddings are trained too? I do get they are based on position instead of the token value, but in they end they are summed... I think I got an intuition; understand it btter
why the learnable parameters (I should be there)
is it true that keeping the normalization enables to find more easily similarities between inputs and features?


TODO connect to arena tests
embed and position embed initialized with a normal, why?